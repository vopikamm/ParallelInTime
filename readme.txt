This branch contains the version of the program that uses a coarse and a fine grid.

For the execution openFoam.zip needs to be unziped in advance. The main program can then be found in parareal_openFoam.py. At the bottom of that file one finds the main function.